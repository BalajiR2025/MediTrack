# MediTrack Frontend (Module 1 — Auth)

This folder contains a minimal Vite + React frontend implementing authentication UI (Register, Login, Profile).

Quick start:

```bash
cd "H:/meditrack project/MediTrack/meditrack-frontend"
npm install
npm run dev
```

API: point `src/api/axios.js` baseURL at your Django backend (e.g. `http://localhost:8000`).
